name: flutter_face_future
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.3
  google_generative_ai: ^0.4.7
  provider: ^6.1.5+1
  lottie: ^3.3.2
  screenshot: ^3.0.0
  path_provider: ^2.1.5
  image_gallery_saver: ^2.0.3
  fl_chart: ^1.1.1
  url_launcher: ^6.3.2
  flutter_dotenv: ^6.0.0
  image_picker: ^1.2.1
  shared_preferences: ^2.5.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
